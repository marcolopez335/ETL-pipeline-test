# Runtime dependencies for the AMMM Jira ETL pipeline.
# Pin to known-compatible major versions; tighten further in CI as needed.

polars>=1.0,<2.0
pyarrow>=15.0
pantab>=5.0
pyyaml>=6.0
rich>=13.0
pandas>=2.0           # Required by run_query (TibcoConnection.execute_query returns pandas)

# Internal package — provides csm_commonlib.database.tibco, csm_commonlib.logging
# and csm_commonlib.tableau.publish. Install from the internal index/wheel.
# csm_commonlib>=X.Y
